import React, { Component } from 'react';
import { ScrollView, View, Text, Image, Button } from 'react-native';

export default class Dinner extends Component {
render(){ 
  return(
   
    <ScrollView style={{flex: 1}}>
        <Text>Dinner list</Text>


    </ScrollView>
  );
}
}
